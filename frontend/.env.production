# Production build — Vite uses this file automatically during `npm run build`.
# This sets the backend API URL for the deployed frontend on Render.
# Do NOT put SMTP credentials or secrets here — this file is baked into the JS bundle.
VITE_API_URL=https://prayan-tutorials-modern-coaching.onrender.com
