## Changes Made

### 1. Corrected the public Prayan Tutorials logo URL
- **File:** `backend/utils/email.py`
- **Change:** Updated `LOGO_URL` to the project's actual public GitHub repository path.
- **Why:** The previous URL returned HTTP 404, so Gmail could not load the remote image.
- **Revert:** Restore the previous `LOGO_URL` only if that old repository path becomes valid again.

### 2. Embedded the logo inline in both enquiry emails
- **File:** `backend/utils/email.py`
- **Change:** Fetch the verified public PNG, place it in each MIME message as an inline image with `Content-ID: <prayan-tutorials-logo>`, and use `cid:prayan-tutorials-logo` in both HTML templates. Fall back to the verified public URL if fetching fails.
- **Why:** Gmail and privacy/image-blocking settings may prevent remote images from loading. Inline CID content makes the logo available directly inside the email.
- **Revert:** Remove `_load_logo_bytes`, `LOGO_CID`, `MIMEImage`, and the `logo_bytes`/CID wiring from `_make_message` and `send_enquiry_emails`; restore HTML image sources to `LOGO_URL`.

## Revert Status
- [ ] Change 1 - Corrected public logo URL
- [ ] Change 2 - Inline logo MIME embedding
